.class ~ .class
has of class
font-size - rem
font-height -rem
letter-spacing:rem ex:(font-size/10%);
width:%, calc()

usually height should not be set explicitly , but if the conditions like to avoid the content shifting or any other scenarios , we can add the min-height

height: vh,rem

for settig left ,right size use padding ,(padding top and bottom also ok but if you have any chance of acheiving the same using the gap(when parent is grid or flex) go for gap rather than padding-top or bottom and adjust the remaining height and bottom with padding )

